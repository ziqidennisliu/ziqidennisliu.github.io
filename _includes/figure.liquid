{% assign img_path = include.path | remove: '.jpg' | remove: '.jpeg' | remove: '.png' | remove: '.tiff' | remove: '.gif' %}
{% assign parts = include.path | split: '.' %}
{% assign ext = parts.last %}
{% assign responsive_raster = false %}
{% if ext == 'gif' or ext == 'jpeg' or ext == 'jpg' or ext == 'png' or ext == 'tiff' %}
  {% assign responsive_raster = true %}
{% endif %}
{% if include.responsive == false %}
  {% assign responsive_raster = false %}
{% endif %}
{% assign fallback_src = include.path | relative_url %}
{% if include.url %}
  {% assign fallback_src = include.url %}
{% elsif site.imagemagick.enabled and responsive_raster %}
  {% assign largest_width = site.imagemagick.widths | last %}
  {% assign responsive_fallback_path = img_path | append: '-' | append: largest_width | append: '.webp' %}
  {% assign fallback_src = responsive_fallback_path | relative_url %}
{% endif %}
{% unless include.url %}
  {% if include.cache_bust %}
    {% assign cache_busted_original = include.path | relative_url | bust_file_cache %}
    {% assign cache_digest = cache_busted_original | split: '?' | last %}
    {% assign fallback_src = fallback_src | append: '?' | append: cache_digest %}
  {% endif %}
{% endunless %}

<figure
  {% if include.slot %}
    slot="{{ include.slot }}"
  {% endif %}
>
  <picture>
    <!-- Auto scaling with imagemagick -->
    <!--
      See https://www.debugbear.com/blog/responsive-images#w-descriptors-and-the-sizes-attribute and
      https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images for info on defining 'sizes' for responsive images
    -->
    {% if site.imagemagick.enabled %}
      <source
        class="responsive-img-srcset"
        {% if responsive_raster %}
          srcset="{% for i in site.imagemagick.widths %}{{ img_path | relative_url }}-{{ i }}.webp {{ i }}w,{% endfor %}"
          type="image/webp"
        {% else %}
          srcset="{{ include.path | relative_url }}"
        {% endif %}
        {% if include.sizes %}
          sizes="{{ include.sizes }}"
        {% else %}
          sizes="95vw"
        {% endif %}
      >
    {% endif %}
    <img
      src="{{ fallback_src }}"
      {% if include.class %}
        class="{{ include.class }}"
      {% endif %}
      {% if include.width %}
        width="{{ include.width }}"
      {% else %}
        width="100%"
      {% endif %}
      {% if include.height %}
        height="{{ include.height }}"
      {% else %}
        height="auto"
      {% endif %}
      {% if include['min-width'] or include['min-height'] or include['max-width'] or include['max-height'] %}
        style="
          {% if include['min-width'] %}
            min-width: {{ include.min-width }};
          {% endif %}
          {% if include['min-height'] %}
            min-height: {{ include.min-height }};
          {% endif %}
          {% if include['max-width'] %}
            max-width: {{ include.max-width }};
          {% endif %}
          {% if include['max-height'] %}
            max-height: {{ include.max-height }};
          {% endif %}
        "
      {% endif %}
      {% if include.alt %}
        alt="{{ include.alt }}"
      {% endif %}
      {% if include.title %}
        title="{{ include.title }}"
      {% endif %}
      {% if include.zoomable %}
        data-zoomable
      {% endif %}
      {% if include.loading %}
        loading="{{ include.loading }}"
      {% elsif site.lazy_loading_images %}
        loading="lazy"
      {% endif %}
      onerror="this.onerror=null; $('.responsive-img-srcset').remove();"
    >
  </picture>

  {% if include.caption %}
    <figcaption class="caption">{{ include.caption }}</figcaption>
  {% endif %}
</figure>
